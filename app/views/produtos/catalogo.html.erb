<%= link_to "Some Page", root_path %>
<h1>Produtos</h1>
<table class="table table-bordered">
    <tbody>
        <tr>
            <th>NOME</th>
            <th>PREÇO</th>
            <th>DESCRIÇÃO</th>
            <th colspan="1">
        <tr>
        <tr>
        <% produto = Produto.all %>
        <% produto.each do |p| %>
            <td><%=p.nome =%></td>
            <td><%=p.preco =%></td>
            <td><%=p.descricao =%></td>
            <td><a href="<%= p.id %>/delete"
            >Remover</a></td>
        </tr>
        <% end %>
    </tbody>    
</table>